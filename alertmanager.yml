global:
  resolve_timeout: 5m

route:
  receiver: "null"
  group_by: ["alertname", "job"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 12h

receivers:
  - name: "null"

# Exemple email (remplacer le receiver dans route par "email") :
# receivers:
#   - name: "email"
#     email_configs:
#       - to: "ops@example.com"
#         from: "alertmanager@example.com"
#         smarthost: "smtp.example.com:587"
#         auth_username: "alertmanager@example.com"
#         auth_password: "change_me"
